/**
 * @version 3.0
 */
/**
 * @author Mahdi, Jaberzadeh Ansari
 * @email mahdijaberzadehansari@yahoo.co.uk
 */
/**
 * @role This package and its classes are responsible for reading or writing the
 *       FIDUS files. When we want to convert a FIDUS file to the DOCX file we
 *       must extract the FIDUS file at first. This package manages this
 *       uncompressing and keeps the actual paths to the uncompressed files.
 *       Also, when we want to convert a DOCX file to the FIDUS file, it is this
 *       package that moderates this compression.
 */
package fidusWriter.fileStructure;