package fidusWriter.model.document;

/**
 * @author Mahdi, Jaberzadeh Ansari
 * @role This enum contains different possible types of nodes in a contents part
 *       of the document.json file
 */
public enum NodeType {
	p, h1, h2, h3, h4, h5, h6, b, i, ul, ol, li, code, blockquote, div, span, figure, figcaption, img, br, pre, a
}
